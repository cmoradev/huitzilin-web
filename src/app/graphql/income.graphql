mutation createIncomes ($input: CreateIncome!) {
  createIncomes (
    input: $input
  ) {
    id
    folio
  }
}

mutation createLinkIncomes ($input: CreateLinkIncome!) {
  createLinkIncomes (
    input: $input
  ) {
    id
    folio
  }
}